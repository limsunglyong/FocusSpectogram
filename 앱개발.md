# 지침

1. 앱 버전 관리 규칙 : v{Major}.{Minor}.{Patch}
   - Major : 사용자 선택 사항.
   - Minor : 기능 추가. 구조 변경.
   - Patch : 버그 수정. 기존 기능 개선 또는 수정.

그외, 애매한 부분에 대해서는 사용자에게 버전 변경을 물어본 뒤 그 내용을 기록한다.
앱 버전을 앱 내의 global 상수로 기록하고 조회, 표시 될 수 있도록 한다.

코드 작성 중 {Minor} 변경 사항에 대해, 모듈/블록 추가에 대해서는 앱 버전을 comment로 기록해둔다.
코드 작성 중 {Patch} 변경 사항에 대해, 수정 내용 부분에 앱 버전을 comment로 기록해둔다.

2. 섹션 : "앱 개발 내용" - 앱 전체 개발에 대한 구체적인 계획 및 내용을 기록한다. (실제 앱 설계서)

3. 섹션 : "앱 버전별/날짜별 개발, 추가, 수정 등의 작업 History" 
   - 실제 개발된 내용을 기록한다. 직접 코드를 여기에 작성하는 일은 최소화 하되, 꼭 필요하다면 추가한다.
   - 모듈별 또는 Phase별로 지난 내용에 대해서는 모두 "앱개발-아카이브.md"로 이관/정리한다.

4. 섹션 : "버그" - 자체적으로 파악한 버그 및 사용자가 보고한 버그 사항을 표로 정리하여 모두 기록한다. 앱 버전과 보고된 날짜를 기록하고 이후 처리 상황에 따라 표를 정리한다.

5. 섹션 : "다음 작업 할 내용 정리" - 위 "앱 버전별/날짜별 개발, 추가, 수정 등의 작업 History" 이후에 진행해야할 작업 내용을 미리 기록해 둔다. 필요하다면 승인 받아야 할 내용도 함께 기록해서 이후 바로 진행 될 수 있도록 내용을 정리한다.

## 앱 개발 계획 (Phase 별)

> 전체 계획·아키텍처·일정·시험 방안은 `개발-프로세스.md` 참조.

| Phase | 개발 항목 | 목표 버전 | 상태 |
|-------|-----------|-----------|------|
| 0 | 프로젝트 셋업 (Vite+React+TS+Tailwind, 디자인 토큰, 버전 상수, 앱 셸) | v0.1.0 | ✅ 완료 |
| 1 | 오디오 입력·디코딩 (decodeAudioData, 메타데이터) | v0.2.0 | ✅ 완료 |
| 2 | STFT/FFT 분석 엔진 (윈도잉, FFT, dB 매트릭스, Web Worker) | v0.3.0 | ✅ 완료 |
| 3 | 3D 시각화 코어 (Three.js 서피스, 컬러맵, OrbitControls) | v0.4.0 | ✅ 완료 |
| - | 3D 뷰 보완 (뷰 리셋, 좌표축 라벨·눈금) [사용자 요청] | v0.5.0~0.5.3 | ✅ 완료 |
| 4 | UI 쉘·패널 연동 (뷰포트 설정 → 3D 실시간 제어) | v0.6.0 | ✅ 완료 |
| 5 | 재생·트랜스포트 동기화 (플레이헤드, 타임라인) | v0.7.0 | ✅ 완료 |
| 6 | Electron 패키징·최적화 | v0.8.0 | ✅ 완료 |
| - | 재생 시점 FFT 단면 강조선 [사용자 요청] | v0.8.1 | ✅ 완료 |
| 7 | EQ 오버레이·실시간 조절 [사용자 요청] | v0.9.0 | ✅ 완료 |
| 8 | 최종 시험·릴리스 | v1.0.0 | 예정(보류) |

> 비고: 사용자 보완 요청(v0.5.x)으로 phase↔버전 매핑이 한 칸씩 이동됨(Phase 5=v0.7.0 …). 최종 릴리스(Phase 8)는 사용자 요청으로 추후로 보류.

### 기술 스택 (확정)
- **빌드/프레임워크:** Vite 5 + React 18 + TypeScript 5
- **스타일:** Tailwind CSS 3 (프로토타입 Emerald & Gold 디자인 토큰 이식)
- **3D:** Three.js (Phase 3 연결 예정)
- **상태관리:** Zustand
- **플랫폼 전략:** 브라우저 우선 → Phase 6 Electron 래핑

## 앱 버전별/날짜별 개발, 추가, 수정 등의 작업 History

### v0.9.0 — 2026-06-23 — EQ 오버레이·실시간 조절 [사용자 요청]
- **Minor(기능 추가):** 5밴드 파라메트릭 EQ를 재생음에 실시간 적용 + 3D 스펙트로그램 앞면에 응답 곡선 오버레이 (**승인: v0.9.0 / 5밴드 / 앞면 고정 / 3D 직접 드래그는 후속**)
- `src/audio/eq.ts` 신설: 밴드 모델(`EqBand`)·기본값(low-shelf 100 / peaking 300·1k·3.5k / high-shelf 8k, 전부 0dB)·Web Audio 사양 계수식 기반 합성 응답(dB) 계산(`computeEqMagnitudeDb`) — 오디오 필터와 오버레이 곡선이 동일 식을 공유해 정확히 일치
- `player.ts`: 시그널 체인을 `source → [BiquadFilter ×5] → gain(master) → destination`으로 변경. `setEqEnabled`/`setEqBands`는 노드 재생성 없이 `AudioParam.setTargetAtTime`(0.01s)으로 갱신해 클릭음 없이 재생 중 조절. off=전 밴드 게인 0(unity 바이패스). `getEqResponseDb(freqs)` 제공
- `SpectrogramScene`: 앞면(time=0, x=-W/2)에 주파수축(Z)을 따라 EQ 합성 응답을 그리는 emerald 곡선 + 0dB 기준선. gain 0dB=HEIGHT/2 중심, ±18dB를 ±HEIGHT/2로 매핑. `setEqVisible`/`refreshEqCurve`, depthTest off로 항상 강조. 스펙트로그램 교체 시 주파수 도메인(0..Nyquist) 재설정
- `appStore`: `eqEnabled`/`eqBands` + `setEqEnabled`/`setEqBand(id,patch)`/`resetEq` — player와 씬에 동시 반영(`SpectrogramCanvas`가 on/off·밴드 변경을 구독해 오버레이 갱신, `eqResponseProvider=player.getEqResponseDb` 연결)
- `EqPanel` 신설(우측 패널군): ON/OFF 토글 + 밴드별 GAIN/FREQ/Q(peaking만) 슬라이더 + Reset EQ. 우측 패널 컬럼은 길이 증가 대비 세로 스크롤 허용
- **검증:** `npx tsc --noEmit` 통과, `npm run build` 성공(68 modules)

### v0.8.1 — 2026-06-23 — 기능개선: 재생 시점 FFT 단면 강조선 [사용자 요청]
- **Patch(기존 기능 개선):** 플레이헤드 위치의 FFT 스펙트럼 단면(주파수축 Z × 강도 Y)을 따라가는 강조선 추가
  - `SpectrogramScene`: 플레이헤드 그룹의 자식 `THREE.Line`(로컬 X=0 → group.position.x로 시간 위치 추종). 매 프레임 `updateSliceLine`에서 해당 시간 열의 서피스 정점 좌표를 복사해 갱신, depthTest off·renderOrder로 항상 강조
  - 서피스와 동일 다운샘플 격자(`buildSpectrogramGeometry`의 cols/rows) 재사용으로 단면이 표면과 정확히 일치
- **검증:** `npx tsc --noEmit` 통과

### feature/lufs 병합 — 2026-06-24 — 기능개선: Hop Size 선택 + LUFS 기준 시각화 [사용자 요청]
- **Patch(기존 기능 개선):** FFT Properties 패널에서 STFT Hop Size를 선택할 수 있도록 변경하고, LUFS 기준 평면/초과 영역 표시 옵션을 추가
- `appStore`: `setStftParams` 액션 추가. Hop Size 변경 시 진행 중 분석을 취소하고 현재 오디오 버퍼가 있으면 새 파라미터로 자동 재분석. `lufsLevel`/`showLufsPlane` 상태와 setter 추가
- `FftPropertiesPanel`: Hop Size 드롭다운(128/256/512/1024/2048), LUFS Level 드롭다운(-6~-40 LUFS 주요 기준값), LUFS Plane on/off 체크박스 추가. Z축 안내를 `LEVEL (LUFS)`로 갱신
- `surface.ts`: LUFS 평면 표시가 켜진 경우 선택 LUFS 기준 이상인 스펙트로그램 vertex 색상을 적색으로 표시하는 옵션 추가
- `SpectrogramScene`: 선택 LUFS 높이를 현재 표면 높이 좌표계에 매핑해 반투명 적색 기준 평면을 표시. LUFS 값/토글 변경 시 현재 스펙트로그램을 재빌드해 평면/초과 색상/축 라벨을 동기화
- `axes.ts`: 세로 레벨 축 제목을 `LEVEL (LUFS)`로 변경하고, LUFS Plane on 시 선택 LUFS 라벨을 적색으로 표시
- `SpectrogramCanvas`: store의 LUFS 설정을 Three.js 씬에 전달
- **검증:** `npm.cmd run lint` 통과, `npm.cmd run build` 통과(66 modules). 브라우저에서 LUFS 컨트롤 렌더링, 기본값 -23 LUFS, Hop Size 기본값 512, 콘솔 오류 없음 확인
- **주의:** 현재 LUFS 초과 표시는 기존 STFT dB 레벨을 선택 LUFS 기준선에 맞춰 시각 비교하는 방식이다. EBU R128 기반 integrated/short-term/momentary LUFS 실제 계산은 별도 DSP 단계로 확장 필요

### v0.8.0 — 2026-06-23 — Phase 6 Electron 패키징
- **Minor(구조 추가):** 브라우저 앱을 Electron 데스크톱 앱으로 래핑 (**승인: electron-builder / Windows 우선**)
- `electron/main.cjs`: 메인 프로세스 — dev는 Vite 서버(`http://localhost:5173`)·DevTools, 배포는 번들 `dist/index.html` 로드(`app.isPackaged` 분기). `ready-to-show` 후 표시, 외부 링크는 기본 브라우저로(`setWindowOpenHandler`)
- `electron/preload.cjs`: contextIsolation 환경에서 `window.soniccube`(platform/versions)만 노출. `nodeIntegration:false`, `sandbox:true`
- `vite.config.ts`는 `base:'./'`가 이미 설정되어 있어 file:// 로딩 호환(추가 변경 불필요)
- `package.json`: `main` 필드, 스크립트 `electron:dev`(concurrently+wait-on)·`electron:build`(NSIS)·`electron:dir`(언팩), `build`(electron-builder) 설정 — appId·productName·files(dist+electron)·win:nsis(설치 경로 선택 가능, oneClick=false)
- devDeps 추가: electron 33, electron-builder 25, concurrently, wait-on
- **검증:** `npm run build`(66 modules) + `electron-builder --win` 성공 → `release/SonicCube Setup 0.8.0.exe`(79.7MB) 및 `release/win-unpacked/SonicCube.exe` 생성. `release/`는 .gitignore 추가
- **비고(샌드박스):** 설치 스크립트 정책으로 electron 바이너리 postinstall이 자동 실행되지 않아 캐시 zip을 수동 추출함. 일반 환경에서는 `npm install` 시 자동 다운로드됨

### v0.7.1 — 2026-06-23 — 기능개선: 지나온 구간(Played Region) 표시 옵션 [사용자 요청]
- **Patch(기존 기능 개선):** 플레이헤드가 지나온 시간 구간의 서피스 표시 방식을 우측 Viewport Settings에서 선택 (SHOW / FADE / HIDE)
  - `SpectrogramScene`: 서피스 `MeshStandardMaterial`에 `onBeforeCompile`로 셰이더 주입 — 플레이헤드 X(uniform) 기준 `vSurfaceX < uPlayheadX`인 프래그먼트를 FADE(알파 0.12) 또는 HIDE(discard) 처리, `transparent=true`
  - 플레이헤드 X를 매 프레임 `updatePlayhead`에서 uniform(`uPlayheadX`)에 전달, `setPlayedMode(0/1/2)`로 모드 전환
  - `appStore`: `playedRegion`('show'|'fade'|'hide') + `setPlayedRegion`, `SpectrogramCanvas`가 모드→씬 연동
  - `ViewportSettingsPanel`: Perspective와 동일 스타일의 3-버튼 토글(SHOW/FADE/HIDE) 추가
- **검증:** `npm run build` 성공(66 modules)

### v0.7.0 — 2026-06-23 — Phase 5 재생·트랜스포트 동기화
- **Minor(기능 추가):** 오디오 재생 + 트랜스포트 컨트롤 + 3D 플레이헤드 동기화
- `src/audio/player.ts` 신설: `AudioBufferSourceNode` + `GainNode` 기반 `AudioPlayer`(앱 전역 단일 인스턴스 `player`)
  - play/pause/stop/seek, 마스터 볼륨(GainNode), 반복(loop)
  - Web Audio가 위치를 제공하지 않으므로 `AudioContext.currentTime` 기준으로 재생 위치 추적(`getCurrentTime`)
  - 수동 정지 시 `onended`를 먼저 null로 만들어 자연 종료 콜백과 구분(자연 종료 시 `onEnded`/`onStateChange` 통지)
  - 디코딩은 원본 레이트(v0.6.1)지만 재생 컨텍스트 레이트와 달라도 Web Audio가 재생 시 자동 리샘플
- `appStore` 확장: `isPlaying`/`isLooping`/`volume`/`duration` + `play`/`pause`/`togglePlay`/`stop`/`seek`/`setVolume`/`toggleLoop`. `player.onStateChange`로 isPlaying 동기화, 로드/clear 시 정지·초기화
- 3D 플레이헤드(**승인: 평면 + 바닥 라인**): `SpectrogramScene`에 반투명 평면(주파수×강도 면) + 바닥 막대(주파수축) 추가, gold 색
  - 시간 도메인 = 서피스 시간축 길이(`frames×timeStep`), 위치 = `playheadTimeProvider()`를 매 프레임 조회해 X로 매핑(React 리렌더 없이 씬 애니메이트 루프에서 갱신)
  - `SpectrogramCanvas`가 provider를 `player.getCurrentTime`으로 연결, 스펙트로그램 유무에 따라 가시성 토글
- `TransportBar` 재구성: 재생/일시정지 토글, 처음으로(skip_previous)·끝으로(skip_next), 반복 토글, 타임라인 스크러버(클릭·드래그 seek), 실시간 타임코드(현재/전체), 볼륨 슬라이더(아이콘 상태 반영). rAF로 재생 중 위치 갱신
- **검증:** `npm run build` 성공(66 modules)

### v0.6.1 — 2026-06-23 — 버그수정: 디코딩 리샘플링으로 주파수축 상한이 PC마다 달라짐 (BUG-002)
- **Patch:** 공유 `AudioContext.decodeAudioData`가 오디오를 하드웨어 기본 레이트로 리샘플링하여, 96kHz 머신에서 48kHz 원본이 96kHz로 업샘플 → 주파수축 상한(Nyquist)이 48kHz로 표시되고 24kHz 이상은 신호가 없던 문제
- `src/audio/decoder.ts`에 `decodeToBuffer` 추가: 원본 헤더 레이트를 알고 공유 컨텍스트 레이트와 다르면 `OfflineAudioContext(1,1,originalSampleRate)`로 디코드해 리샘플링 없이 원본 레이트 유지
  - `arrayBuffer.slice(0)` 사본으로 시도 → 오프라인 디코드 실패 시 원본 버퍼로 공유 컨텍스트 폴백(detach 방지)
  - 원본 레이트 미파악(헤더 실패)/레이트 동일 시 기존 공유 컨텍스트 경로 유지
- 결과: 어느 PC에서나 48kHz 원본 → `analysisSampleRate=48000` → 축 0~24kHz로 통일. `sampleRateIsOriginal=true`일 때 `sampleRate`와 `analysisSampleRate` 일치
- **검증:** `npx tsc --noEmit` 통과

### v0.6.0 — 2026-06-22 — Phase 4 UI 패널 연동
- **Minor(기능 추가):** Viewport Settings 패널 ↔ 3D 카메라 양방향 연동
- `SpectrogramScene` 카메라 제어 API:
  - `setRotationX(deg)`: 슬라이더 각(0~90°) → 카메라 극각(지평선 기준 올려본 각)
  - `setZoom(z)`: 원근=카메라 거리(BASE_RADIUS/zoom), 정사영=`OrthographicCamera.zoom`
  - `setPerspective(mode)`: **ISO**(등각 프리셋 θ45°/elev35°)·**ORTHO**(정사영 카메라 전환)·**3D**(자유 원근)
  - 원근↔정사영 전환 시 위치/방향 이어받아 OrbitControls 재생성(`rebuildControls`)
  - `onCameraChange` 콜백: 드래그 등 카메라 변경을 rotationX/zoom으로 역산해 보고
- `SpectrogramCanvas` 양방향 동기화: store→씬(effect), 씬→store(onCameraChange), `lastFromScene` 가드로 피드백 루프 차단
- Rotation X/Zoom 슬라이더·Perspective 버튼이 실시간으로 3D 뷰 제어, 마우스 조작 시 슬라이더도 갱신
- **검증:** `npm run build` 성공(65 modules), dev 서버 모듈 HTTP 200

### v0.5.3 — 2026-06-22 — TIME 축을 0Hz 원점 모서리로 이동 [사용자 요청]
- **Patch:** TIME 축(시간 0 시작점)을 FREQUENCY 원점(0Hz) 모서리에서 시작하도록 이동
  - `axes.ts` TIME 축선/눈금/제목을 z=+DEPTH/2 → **z=-DEPTH/2**로 이동
  - 결과: TIME·FREQUENCY·INTENSITY 세 축이 원점 모서리(x=-W/2, z=-D/2)에서 교차
- **검증:** `npm run build` 성공(65 modules)

### v0.5.2 — 2026-06-22 — 축 라벨 위치/크기 + 주파수축 재반전 [사용자 요청]
- **Patch:** v0.5.1 축에 대한 추가 보완
- 1) 축 이름을 각 축의 **끝(최댓값 쪽)** 으로 이동, 폰트 크기를 **눈금 스케일과 동일**하게(`TITLE_TEXT_H = TICK_TEXT_H`)
  - TIME→시간 최대 끝, FREQUENCY→주파수 최대 끝, INTENSITY→상단 끝
- 2) 주파수축 방향 재반전: 0Hz를 z=-DEPTH/2, 고주파를 z=+DEPTH/2 (`surface.ts` z 매핑 + `axes.ts` zOfFreq 동시)
  - INTENSITY 축을 0Hz·0s 원점 모서리(x=-W/2, z=-D/2)로 이동해 정합
- **검증:** `npm run build` 성공(65 modules)

### v0.5.1 — 2026-06-22 — 축 라벨 개선 + 주파수축 방향 수정 [사용자 요청]
- **Patch:** v0.5.0 좌표축에 대한 사용자 보완 요청 반영
- 1) 축 이름 스타일: 라운드 사각형 박스(반투명 배경 + 축 색 테두리) 안에 표기, 폰트 normal(400) 가중치, 크기 약 50% 축소(글자 월드 높이 6→3)
  - `axes.ts`의 `makeLabel`에 `boxed` 옵션·`roundRectPath` 추가, 글리프 기준 스케일 산식으로 박스 유무와 무관하게 글자 크기 일관화
- 2) 주파수축 방향 반전: 기존 0Hz가 뒤쪽(원점 반대편)에 있던 것을 **0Hz를 원점 모서리(TIME·FREQUENCY 축 교차점, z=+DEPTH/2)** 로 이동
  - `surface.ts` 지오메트리 z 매핑 반전(`0.5 - r/(rows-1)`) + `axes.ts` `zOfFreq` 반전으로 메쉬·축 정합 유지
- **검증:** `npm run build` 성공(65 modules)

### v0.5.0 — 2026-06-22 — 3D 뷰 보완 (뷰 리셋 + 좌표축 라벨) [사용자 요청]
- **Minor(기능 추가):** 사용자 보완 요청 2건 반영 (Phase 4 UI 연동의 일부)
- 1) 뷰 리셋: `SpectrogramScene.resetView()`로 카메라 위치·타깃을 기본값으로 복원
  - store `viewResetNonce`/`requestViewReset()` 신호 → `SpectrogramCanvas`가 구독해 호출
  - Viewport Settings 패널에 "Reset View" 버튼(restart_alt 아이콘) 추가
- 2) 좌표축 라벨·눈금: `src/viz/axes.ts` 신설
  - X=TIME (s), Z=FREQUENCY (Hz), Y=INTENSITY (dB) 축선 + 제목 + 눈금 스케일
  - 텍스트는 canvas 텍스처 스프라이트(항상 카메라 향함, depthTest off), 축별 색상(time=gold, freq=emerald, intensity=light)
  - 눈금 값: 시간 0~duration, 주파수 0~Nyquist(k 단위 축약), dB는 -90~maxDb
  - 기존 `AxesHelper`(RGB 기본축) 제거, 라벨 축으로 대체. setSpectrogram 시 재생성/dispose
- **검증:** `npm run build` 성공(65 modules)

### v0.4.0 — 2026-06-22 — Phase 3 3D 시각화 코어
- **Minor(기능 추가):** 스펙트로그램 → Three.js 3D 서피스 렌더링
- 의존성: `three` / `@types/three` 사용 (Phase 0에서 설치 완료)
- `src/viz/colormap.ts`: 정규화값 → 히트맵 그라디언트(deep navy→emerald→gold→light), 프로토타입 범례와 동일
- `src/viz/surface.ts`: 스펙트로그램 → BufferGeometry
  - **시간축 max-pooling 다운샘플(최대 2000 프레임, 사용자 승인)** + 주파수축 다운샘플(내부 캡 512 bin)
  - 정점 변위(높이=레벨 dB, -90dB 바닥 정규화) + 정점 색상, 정점 수 따라 16/32bit 인덱스 자동 선택
  - 축 매핑: X=시간, Z=주파수(깊이), Y=레벨(높이)
- `src/viz/SpectrogramScene.ts`: 씬/카메라/WebGLRenderer/OrbitControls(댐핑), 그리드+AxesHelper, 조명, 애니메이션 루프, ResizeObserver 리사이즈, dispose 정리
- `src/ui/SpectrogramCanvas.tsx`: 씬 마운트/해제 + store `spectrogram` 변경 시 메쉬 갱신
- `Viewport`: 분석 완료(`analysisStatus==='done'`) 시 3D 캔버스 렌더, 그 외 상태 텍스트 유지. 패널은 z-10 오버레이
- 빌드 최적화: `vite.config.ts` manualChunks로 Three.js 벤더 청크 분리(three 457KB / app 182KB)
- **검증:** `npm run build` 성공(64 modules), dev 서버 viz 모듈 HTTP 200

### v0.3.0 — 2026-06-22 — Phase 2 STFT/FFT 분석 엔진
- **Minor(기능 추가):** 오디오 → 스펙트로그램(시간×주파수×dB) 분석 파이프라인 구현
- `src/audio/fft.ts` 신설: 외부 의존성 없는 Radix-2 Cooley-Tukey FFT. bit-reversal/twiddle 사전계산으로 프레임 반복 최적화
- `src/audio/stft.ts` 신설: STFT 엔진
  - 윈도우 함수 Hann/Hamming/Blackman, 프레임 분할(fftSize/hopSize)
  - 매그니튜드 → dBFS 변환(윈도우 합 기반 진폭 정규화, full-scale 사인 ≈ 0dBFS), 하한 -120dB 클램프
  - 결과 `Spectrogram`: frames×bins 행렬 + timeStep/freqStep/minDb/maxDb 메타
  - **기본 파라미터(사용자 승인): FFT 2048 / hop 512 / Hann**
- `src/audio/stft.worker.ts` + `src/audio/analyzer.ts` 신설: Web Worker로 비동기 분석(진행률 콜백, transferable 버퍼로 복사 최소화), 모노 믹스는 메인 스레드에서 수행
- `appStore` 확장: `stftParams`/`spectrogram`/`analysisStatus`(idle·analyzing·done·error)/`analysisProgress`/`analysisError`. 디코딩 성공 시 자동 분석 시작, 새 파일 로드/clear 시 진행 중 분석 취소
- UI: Viewport 중앙에 분석 진행률 바·결과 요약(frames×bins, dB 범위), FftPropertiesPanel에 Window/FFT Size/Hop Size 및 Δt·Δf 표기, ANALYZING 배지
- **검증(A2):** 테스트 톤(440Hz/1kHz) 독립 DFT 피크 검증 — 두 피크 모두 기대 주파수와 0.5 bin 이내 일치(`scripts/verify-fft.mjs`). `npm run build` 성공(58 modules, 워커 청크 분리)

### v0.2.1 — 2026-06-22 — 버그수정: Sample Rate 원본값 표시 (BUG-001)
- **Patch:** `decodeAudioData`가 오디오를 AudioContext 레이트로 리샘플링하여 `buffer.sampleRate`가 항상 컨텍스트 기본값(48kHz)으로 고정되던 문제 수정
- `src/audio/decoder.ts`에 원본 샘플레이트 헤더 파서 추가
  - WAV: RIFF `fmt ` 청크 파싱 / MP3: ID3v2 스킵 후 첫 프레임 헤더 파싱(MPEG 버전·SR 인덱스)
  - 파싱은 `decodeAudioData`가 ArrayBuffer를 detach하기 전에 수행
  - 헤더 파싱 실패 시 분석 레이트로 폴백하고 패널에 `≈` 접두 표기
- `AudioMeta` 확장: `sampleRate`(표시용 원본) / `analysisSampleRate`(=buffer.sampleRate) / `sampleRateIsOriginal` 분리
- `FftPropertiesPanel`이 원본 샘플레이트를 표시
- **검증:** 테스트 WAV 헤더 파서가 44100 반환 확인, `npm run build` 성공(55 modules)

### v0.2.0 — 2026-06-22 — Phase 1 오디오 입력·디코딩
- **Minor(기능 추가):** 오디오 파일 입력 및 디코딩 파이프라인 구현
- `src/audio/decoder.ts` 신설: `File → arrayBuffer → AudioContext.decodeAudioData → AudioBuffer`
  - 메타데이터 추출: 샘플레이트, 채널 수, 길이(초), 총 샘플 수
  - 피크 레벨(dBFS) 산출(전 채널 최대 절대 진폭 → 20·log10)
  - 비트뎁스는 Web Audio 디코딩 특성상 항상 `32-BIT FLOAT`로 표기
  - 에러 처리: 미지원 확장자/빈 파일/디코딩 실패를 `AudioDecodeError`로 사용자 메시지화
  - 공유 `AudioContext` 재사용 (탭당 컨텍스트 수 제한 회피)
- `src/audio/filePicker.ts` 신설: 숨김 input 기반 파일 선택 다이얼로그
- `appStore` 확장: `audioBuffer`/`meta`/`loadStatus`(idle·loading·ready·error)/`loadError` + `loadFile()`/`clearAudio()`
- 입력 UI: TopBar "Upload File" 버튼 연결(로딩 중 비활성), Viewport 전체 영역 드래그&드롭(오버레이 표시)
- 메타데이터 바인딩: `FftPropertiesPanel`(Sample Rate/Bit Depth/Peak Level + 상태 배지), `TransportBar`(파일명/Timecode), Viewport 중앙 상태 표시(idle/loading/ready/error)
- 지원 포맷 우선순위 확정: **mp3, wav 우선** / flac·ogg·m4a·aac는 브라우저 디코더 지원 범위
- 테스트 자산: `scripts/gen-test-tone.mjs` + `assets/test-tones/sine_440_1k_stereo.wav`(L=440Hz, R=1kHz, 3s) — Phase 2 FFT 정합성 검증에도 활용
- **검증:** `npm run build` 성공(55 modules), dev 서버 HTTP 200

### v0.1.0 — 2026-06-22 — Phase 0 프로젝트 셋업
- **Minor(구조 신설):** Vite + React + TypeScript + Tailwind 프로젝트 초기 구성
- 디자인 토큰 이식: 프로토타입(`1. prototype design/`)의 Emerald & Gold 색상/타이포(Geist, JetBrains Mono)를 `tailwind.config.js`로 이식, `index.css`에 glassmorphism 유틸(`.glass-panel`, `.inner-glow-gold`) 정의
- 전역 버전 상수 `src/version.ts` 신설 (`APP_NAME`, `APP_VERSION`, `APP_VERSION_LABEL`) — 콘솔 및 하단 트랜스포트 바에 표시
- 폴더 구조 확정: `src/audio`(처리), `src/viz`(3D), `src/ui`(컴포넌트), `src/store`(상태)
- 앱 셸 구현 (프로토타입 레이아웃 정적 구현): `TopBar`, `SideBar`, `Viewport`(3D placeholder), 패널 3종(`FftPropertiesPanel`/`ViewportSettingsPanel`/`HeatmapPanel`), `TransportBar`
- 뷰포트 설정 슬라이더(Rotation X / Zoom / Perspective)를 Zustand store와 연결 (3D 반영은 Phase 4)
- **검증:** `npm run build`(tsc + vite) 성공, dev 서버 HTTP 200 정상 응답

## 버그 

| # | 버전 | 보고일 | 내용 | 심각도 | 상태 |
|---|------|--------|------|--------|------|
| BUG-001 | v0.2.0 | 2026-06-22 | FFT Properties의 Sample Rate가 원본과 무관하게 48.0kHz로 고정 표시됨 (decodeAudioData 리샘플링이 원인) | Major | ✅ v0.2.1에서 수정 |
| BUG-002 | v0.6.0 | 2026-06-23 | 주파수축 상한이 원본 Nyquist(48kHz→24kHz)가 아닌 PC 하드웨어 레이트 기준으로 표시됨(96kHz 머신에서 축이 0~48kHz, 24kHz 이상 신호 없음). decodeAudioData 리샘플링이 원인 | Major | ✅ v0.6.1에서 수정 |

## 다음 작업 할 내용 정리

### EQ 후속 검토(선택) — v0.9.0 구현 완료(History 참조)
- 3D 오버레이의 컨트롤 포인트 직접 드래그(레이캐스트)로 gain/freq 조절 — 1차에서 제외, 후속 단계
- EQ 적용 후 스펙트럼 미리보기(실시간 재분석 또는 서피스에 gain 곡선 곱) — 비용/복잡도 검토 필요
- 프리셋(flat/low-cut 등) 저장·불러오기, 밴드 솔로/바이패스 개별 토글
- 오버레이 ±dB 표시 범위(현재 ±18dB) 사용자 조절 옵션, 0dB 기준선 라벨

### Phase 8 — 최종 시험·릴리스 (v1.0.0, 사용자 요청으로 보류)
- [ ] 전체 기능 통합 시험(로드→분석→3D→재생→Played Region→EQ), 다양한 포맷/길이/샘플레이트 검증
- [ ] 패키징 앱(설치본) 실기기 동작 확인, 시작 성능·메모리 점검
- [ ] 릴리스 노트·README 정리, 버전 v1.0.0 승격(Major는 사용자 승인 필요)
- **승인 필요:** v1.0.0 승격 시점 / 릴리스 배포 채널

### Phase 6 후속 검토(선택)
- 앱 아이콘 지정(현재 기본 Electron 아이콘) — `build.win.icon`에 .ico 추가
- 오프라인 대비: index.html의 Google Fonts(Material Symbols/Geist/JetBrains Mono) CDN 의존을 로컬 번들로 전환 검토
- 코드 서명(현재 미서명 → SmartScreen 경고), 자동 업데이트(latest.yml 생성됨) 채널 구성
- macOS/Linux 타깃 확장(현재 Windows 전용)

### Phase 5 후속 검토(선택)
- 정사영(ORTHO)에서 시간축이 화면에 평행할 때 플레이헤드 평면 가독성 점검
- 매우 짧은/긴 오디오에서 스크러버 드래그 정밀도, seek 시 클릭음(재시작) 여부 점검
- 재생 중 새 파일 로드 시 상태 전환 회귀 점검

### 잔여(낮은 우선순위)
- HEATMAP ANALYZER 범례를 실제 dB/LUFS 표시 정책과 연동(옵션)
- 실제 EBU R128 기반 integrated/short-term/momentary LUFS 계산을 DSP 단계로 추가할지 검토
- 좌측 DSP Tools 사이드바 탭 활성/패널 토글 기본 동작

### 알려진 항목 / 추후 고려
- 주파수축 내부 캡 512 bin: 저사양에서 추가 조정 가능
- 축 눈금 라벨이 회전 각도에 따라 메쉬에 가려질 수 있음(depthTest off로 완화) — 필요 시 빌보드 위치 보정 검토


